"""Phase 3: Entity discovery and mapping."""

from __future__ import annotations

import argparse


def run(args: argparse.Namespace) -> int:
    """Discover entities from parsed contracts."""
    print("Phase 3: discover-entities — not yet implemented")
    return 1
